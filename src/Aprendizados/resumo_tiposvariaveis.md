## 🧗 Tipos de Variáveis :

Depois de algumas informações básicas sobre Python , está na hora de aprender sobre tipos de variáveis existentes no Python .
Então bora lá continuar a nossa jornada ! <br>
Lembrando que apartir daqui irá ser algo mais direto 😄 <br>

- Quando o assunto são tipos de variáveis , na cabeça de um programador em python aparece logo as variáveis do tipo `inteiros`, do tipo `string` , do tipo `float` e do tipo `booleano` .
```python
var_string1 = "Roberto"
var_string2 = "16"
var_inteiro = 14
var_float = 14.5
```
Podemos identificar que qualquer tipo de variável contenha algo dentro de `" "` ou `' '` será do tipo string , para além de variáveis que recebem valores sem `" "` ou `' '` serão inteiros ou booleanas ou do tipo float . Uma maneira fácil de identificar o tipo de variávei é usando `type(var)` :
```python
var_string1 = "Roberto"
var_string2 = "16"
var_inteiro = 14
var_float = 14.5
var_booleano1 = True
var_booleano2 = False

print(type(var_string1)) # Irá retornar o tipo da variável que está dentro de ()
print(type(var_string2)) # Irá retornar o tipo da variável que está dentro de ()
print(type(var_inteiro)) # Irá retornar o tipo da variável que está dentro de ()
print(type(var_float)) # Irá retornar o tipo da variável que está dentro de ()
print(type(var_booleano1)) # Irá retornar o tipo da variável que está dentro de ()
print(type(var_booleano2)) # Irá retornar o tipo da variável que está dentro de ()
```

---

## 🆘 Dica Fundamental :
- Não estude apenas , pratique também ou veja esta tabela e tire suas próprias conclusões -> [Tabela](https://images.app.goo.gl/pnpZWhSfrrYvaxry8)